## Open PQL (Poker Query Language) — User Interface

> **Note:** Vibe coding only repo

## Deploy to Cloudflare

Production runs fully static on Cloudflare Pages (WASM, no daemon).

```
just deploy-preview   # deploy to preview branch
just deploy           # deploy to production
```

See [`deploy/README.md`](deploy/README.md) for first-time setup.
